# F independent bounded-repair audit — candidate 70d25f7

Offline provider simulation with real disposable PostgreSQL; no real credentials loaded and no external network calls. New frozen held-out preparation is under route-held-out/, not executed.

`ai-repair.acceptance-spec.ts` independently exercises actual AiReviewService, provider validators and MeteredAiClient with fake HTTP envelopes and durable database receipts. Six cases pass: configured-only routing preserves the original 429; structural repair uses the existing fallback slot and retains original input; rejected model text is absent from repair instructions; a second invalid output adds no third provider call; availability fallback shares the same two-call ceiling; real target-grammar rejection is stored as failure, with charged usage retained. A normal worker job also fails terminally after three rounds, with attempts 1–6 and no seventh network request (included among those six tests).

**F-REPAIR-01 remains open.** The seventh case models process interruption after two paid receipts are finalized but before AiWorkerService.fail updates retryCount. It expires only the disposable job's lock, reclaims it and repeats. On candidate 70d25f7, six requests have attempt numbers `[1,2,1,2,1,2]`; a fourth round is still admitted. The final test now invokes worker.poll for cleanup instead of forbidding an exhausted lease token: it reproduces **8 calls, attempts `[1,2,1,2,1,2,1,2]`, final QUEUED**. Expected: 6 calls, attempts `[1,2,3,4,5,6]` and terminal FAILED. This permits parent to return an exhausted lease for terminal quota cleanup without allowing more network calls. Thus the candidate's six-call limit holds for orderly exception handling, but not crash recovery. The grammar lease must consume bounded rounds at admission or use another atomic per-job limit that survives this window. Preserve independent budgets for legitimate additional corrections in the same study session. Parent owns the business fix; F changes only tests/reports.

The source audit found AiReviewService.assertSuggestions after provider metering. Its current target rejection is also checked by the same target predicate inside both real providers' validateReviewLanguage callbacks, before receipt success is finalized. The independent invalid-target test yields two `success=false / AI_TARGET_GRAMMAR_MISSING` receipts. No reachable success-receipt/post-validation rejection was reproduced on this candidate. The alternative-sentence fallback is an existing accepted-result mutation, not a thrown rejection. Future validation added after metering must not silently mark rejected feedback successful.

validationFeedback maps schema paths to fixed strings; it does not echo Zod messages, HTTP bodies or rejected model response text. Arbitrary ProviderError.validationFeedback is an internal interface, so its callers must continue deriving content solely from that fixed mapper or fixed generic messages. The test uses a unique hostile response marker and confirms it never reaches the second request or usage rows.

Reproduce: `npx jest --config test/sentence-lab/jest.json --runInBand --runTestsByPath test/sentence-lab/ai-repair.acceptance-spec.ts`. Recorded result: **6 passed / 1 failed**, 1.011s. The failure is intentional contract evidence, not disabled or marked todo. The parent is implementing the lease fix. Candidate semantic qualification also remains pending; the known non-thinking semantic failures are not resolved by a budget cap.

Parent clarified the existing explicit manual retry: after the three automatic rounds, the user may request one final round. The intended bounds are therefore **automatic recovery <=6 calls; per-job lifetime <=8 calls including that explicit manual round**, with two calls shared by repair/fallback per round. The parent fix will preserve that recovery action. Earlier statements of an absolute six-call lifetime limit must be read with this correction. Separate legitimate corrections in one session retain separate job budgets.

## Closure on parent fix 0bf99ed

F independently cherry-picked the parent admission fix and reran the original reproducer: all seven tests pass. Further `ai-recovery.acceptance-spec.ts` coverage verifies concurrent manual retry, a final two-call round with ordinals7/8, terminal quota release, and an overlapping late old claimant that cannot replace a newer result or consume quota again. The full current F suite including card-quote compatibility is 49/49 across ten suites. F-REPAIR-01 is closed for this candidate; prior failed observations remain above as historical evidence.
