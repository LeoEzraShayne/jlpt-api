# F route held-out preparation (not executed)

Twelve frozen inputs, zh/en six each, are in cases.json. They were prepared independently before the next thinking candidate is frozen and have not been sent to D. They differ from D's known corpus and F's earlier real-run fixtures. This file and prepare.py perform no provider calls and load no credentials. The candidate commit and real-run results must be stored separately after parent authorization; never replace previous held-out evidence.

Coverage: incorrect connection plus natural control; positive obligation versus absence of obligation (translation must not invert this into prohibition); target vocabulary used correctly versus an acceptable natural synonym with no target lexical evidence. Score caps follow the existing contract; semantic translation review allows equivalent wording rather than keyword-matching one reference translation. For a natural alternative, null target/meaning/reading evidence is required and the original remains unchanged. A provider failure is recorded as failure, never as an asserted success.

The candidate runner must call the actual AiReviewService.review route and VocabularyAiService operation, not a one-shot low-level provider. A first rejected response followed by a valid response may satisfy the operation; all rejected attempts remain metered and counted in its latency/cost. A repair must receive the same original input. For the erroneous connection, no later attempt may grade the model's corrected sentence as the learner's original. Reject false positives even if structure and score sum pass. Do not alter components, thresholds or expected meanings to pass a run.

Before live evaluation, inspect shared two-call budget across repair/fallback, configured-provider filtering, fixed validation feedback, request UUIDs, thinking accounting, unknown usage and each worker's outer retry ceiling. Twelve operations can consume up to twenty-four network requests at two per operation; this document authorizes none. Parent must authorize the candidate run/budget separately. Model defaults, schema, billing, browser and production remain out of scope.

Per-run artifacts should record candidate commit, fixture SHA256, actual requested/returned model, locale/case ID, operation ID, attempt/request IDs, raw final candidate text without thought content, strict-validation status, F semantic judgment, normalized/raw numeric usage, paid estimate and total latency including all attempts. Every final accepted response is reviewed; absent scenario cannot establish scenario success. Report the narrow sample outcome, not a JLPT-wide accuracy percentage.

Worker-budget clarification from parent: three automatic rounds permit at most six network calls; the existing explicit manual recovery may add one final two-call round, making eight the per-job lifetime upper bound. New legitimate corrections in the same session have distinct job budgets. The pending parent lease fix must enforce admission across crash recovery; this preparation does not assume it is already implemented.
