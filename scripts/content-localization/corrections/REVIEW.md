# F language findings — source and English remediation

Batch `F-language-20260913`. This is C's source-backed editorial remediation, pending independent F recheck. It corrects inherited Chinese/source defects as well as English. No provider API calls were made for this corrective pass; the original 92-call usage audit is unchanged. Review provenance identifies the coding agent, not a human specialist or a new DeepSeek call.

## Decisions and sources

| Finding | Corrected behavior | Evidence checked |
|---|---|---|
| F-L01 | Godan verbs take dictionary form before まい: 行こうか行くまいか. Ichidan dictionary/stem variants, する variants and 来る readings are stated consistently. The incorrect a-row instruction is removed. | [Shogakukan Digital Daijisen, まい](https://kotobank.jp/word/%E3%81%BE%E3%81%84-632809); [NHK research report, December 2018, pp.53–55](https://www.jstage.jst.go.jp/article/bunken/68/12/68_46/_pdf/-char/en) supports godan terminal form and distinguishes ichidan/irregular variation. |
| F-L02 | Plain-form verb/i-adjective alternatives include affirmative, negative and past. Nouns and na-adjective stems take direct or である connection; negative/past copulas remain possible; nonpast affirmative だ is excluded. Existing 行くにしても行かないにしても example remains intact. | [N1et original teacher lesson on paired にしても](https://jn1et.com/nisiro-nisiro/) explicitly contrasts the word classes and affirmative/negative alternatives. |
| F-L03 | Replace the incoherent obligation example with 上司に頼まれたので、引き受けないわけにはいかない。 / 上司拜托了，所以不能不接受。 / My boss asked me, so I have to accept. | [Original Japanese teaching explanation and examples](https://mainichi-nonbiri.com/grammar/n3-wakenihaikanai/) distinguishes inability to do from obligation to act. The replacement is original editorial wording, not a copied source example. |
| F-L04 | Base わけにはいかない entry explains affirmative nonpast connection, including Vている, as cannot do. It explicitly contrasts Vない + わけにはいかない as must do and points to the separate negative entry. | Same [original teaching explanation](https://mainichi-nonbiri.com/grammar/n3-wakenihaikanai/); the explicit polarity distinction is C's teaching clarification. |
| Advisory | Relation English now says がてら takes the opportunity to do B while doing A, preserving the original Chinese 顺便 nuance. Chinese relation source is unchanged. | Existing supplied Chinese relation text; no new grammatical claim. |

The original Japanese title, source IDs, ordering, membership, learning history and grammar/example associations remain unchanged. The one Japanese example sentence changes in place; no user-written sentence or stored AI result changes.

## Artifacts and version semantics

- `F-language-20260913.json`: exact before/after scalar source values and hashes for five records (three grammar text corrections, one example, and that example's parent grammar import hash). Also retains all five previous English records with their old provenance and replacements.
- `../translations.en.jsonl`: exactly 546 current entries. Four source-bound translation hashes changed (three grammar + one example). The relation English revision retains its source hash because its Chinese source is unchanged.
- `../../../data/N1-N5语法总结-有解释例句.txt`: corrected source file; the three explicit connection rules are added so re-import does not lose them. The four affected GrammarPoint.sourceHash values are generated by the existing grammar parser from this corrected source file. This import fingerprint differs intentionally from the content-localization hash, whose inputs are defined by content-source.ts.
- `verification.json`: isolated production-ID corpus verification; 546 active entities and 550 stored translation versions after correction, including four old hash versions. The previous relation English is retained in the correction artifact and the committed ImportBatch summary because ContentTranslation has only one row per source hash/locale.

## Main-agent deployment commands

After the main agent has applied the additive schema migration and backed up production:

1. `npx tsx scripts/content-localization/correct-sources.ts` previews the five source states and validates all existing affected English versions. No writes.
2. `npx tsx scripts/content-localization/correct-sources.ts --commit` atomically applies the guarded source edits, corresponding English and a deduplicated ImportBatch audit. Existing old hash rows remain; missing old translation rows are not fabricated. This supports both a pre-English-import production DB and a DB that already has the original English corpus.
3. Run the previously documented `prepare-scenarios.ts --commit`, followed by `manage.ts --apply` / `manage.ts --apply --commit`, using the corrected 546-entry current artifact.

Each source must match every recorded old scalar value or every recorded new scalar value. Unknown changes fail closed. Existing English fields, status, validation time and provenance must match an old or new reviewed version. A transaction/advisory lock prevents interleaving. A second correction call recognizes the exact new values, does not modify timestamps and does not add another audit. No rollback-to-old-content command is supplied because it would restore known teaching errors; previous values remain inspectable for operational recovery under main-agent review.

## F controlled snapshot workflow

Do not replace F's historical evidence or old production snapshot. Create a separate corrected copy:

`npx tsx scripts/content-localization/correct-sources.ts --snapshot /path/to/original-public-snapshot.json --output /path/to/new-corrected-snapshot.json`

Input and output must differ; the output must not exist. This performs no DB writes, guards the five source rows against the artifact and changes only those rows in a deep copy. It works with either the original 62-scenario snapshot or the catalog-augmented 94-scenario snapshot. Use the corresponding corrected copy for F's new coverage/source review; keep original findings and hashes as historical evidence.

C generated `/Users/shen/Downloads/jlpt/.local/production-static-content-corrected-20260913.json` from the catalog-augmented source (546 entities). It is a new protected file; neither F evidence file was modified. For an original 514-entity acceptance test use the original production snapshot as input to produce a distinct new copy instead.

## Verification completed

Build and targeted ESLint pass; full unit suite: 47 suites / 382 tests pass.

The disposable localhost `jlpt_sentence_lab_content_prodverify_20260913` was used exclusively. Dry-run did not mutate the DB; unexpected source text and unexpected translation provenance each caused atomic rejection; a subsequent successful correction preserved unrelated source rows; repeated corrections preserved exact serialized state including timestamps; all old translation-hash rows remained intact; one ImportBatch audit retained both old and new versions. Current corpus coverage is 546/546, and all 223 grammar/example English reads resolve. The technical checks are separate from F's pending independent linguistic recheck.
