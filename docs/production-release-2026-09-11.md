# 2026-09-11 生产发布记录（日本时间）

本次依据用户后续明确授权执行生产部署及数据库同步。此前开发验收文档中的
“不包含生产发布”描述的是上一次任务边界，不代表本次未发布。

## 配套版本与发布方式

- 后端运行版本：`366aa8254681477498a1738eb2adbfa8527f3b03`。
- 前端运行版本：`74bc6bf`，Next.js 16.3.4。
- Cloudflare Worker：`jlpt-web`，版本 `28707374-52bd-4ea1-967d-5ea8946195f4`。
- 网站：<https://jlpt.meritledger.org>；API：<https://api.jlpt.meritledger.org/api/v1/health>。
- 后端发布目录：`/var/www/jlpt-releases/366aa8254681`。
  `/var/www/jlpt-api` 现为指向该版本的链接；PM2 已 reload 并保存配置。

检查时，GitHub 推送没有使线上更新：两个仓库的 CI 在依赖审计处失败，其他
构建和测试已通过；服务器和 Worker 仍运行旧版。本次修复依赖后，通过 SSH／PM2
与 OpenNext／Cloudflare 完成实际部署，未新增自动部署流水线。

依赖修复未降低审计门槛，两端完整审计均为零漏洞。GitHub 检查已通过：
[后端 CI](https://github.com/LeoEzraShayne/jlpt-api/actions/runs/34492420918)、
[前端 CI](https://github.com/LeoEzraShayne/jlpt-web/actions/runs/34493002483)。
后续仅更新发布记录的提交不改变上述运行代码。

## 备份、演练与正式迁移

生产备份保留在服务器私有目录：
`/home/ubuntu/jlpt-deployment-backups/20260910T145330036Z/production-before.dump`。
大小 270,341 字节；SHA-256：
`754d50cd83a444f0cecc0d70a2056d28bfbc4be3ac083d5e6078a630558d3bf3`。
备份为 PostgreSQL 自定义格式，已校验目录并成功恢复到独立演练库。

在生产副本上完成五个新迁移，核对历史 ID 集合，再用新后端检查计划、今日安排、
词汇、私人导入、表达和复习接口，六项均返回 200，随后才执行正式迁移。

生产和本地开发库现均完成 14 个迁移。逐一核对迁移校验值，以及表字段、索引、
外键、检查约束和枚举；结构指纹完全一致：
`6afd81f15fa62e0a3bae6b4a664f6941f44d823c5098589c68f2f25808effffa`。
Prisma 实际数据库与 schema 比较也无差异。

本地 `.env` 原先指向不存在的 `jlpt_app` 账号，现指向已验收的
`jlpt_v2_integration_20260910` 本地数据库，原配置另行私密备份。同步的是结构与
学习内容；未用本地测试账号或学习历史覆盖生产数据。

| 生产数据 | 迁移前后数量 |
| --- | ---: |
| 用户 | 1 |
| 学习计划 | 1 |
| 学习任务 | 154 |
| 学习会话 | 155 |
| 复习记录 | 154 |
| 句子提交 | 247 |
| AI 批改任务 | 247 |
| AI 批改结果 | 221 |
| 语法进度 | 40 |

以上每类原始 ID 均保留。原 N1 进行中计划、每日 60 分钟配置保持原值。
新版掌握规则已仅为当前唯一用户启用，历史证据仍保留旧版本，不补造场景证据。

## 内容与上线验证

生产与本地均有 15,407 条开放词典读音／义项记录、12 个场景及 2,253 条私人候选。
私人资料归属生产账号，仍为待核对状态；未将候选直接发布或当作已掌握内容。

- 本地补丁验证：后端 183 项单元、30 项真实数据库集成；前端 25 项单元／组件、
  135 项浏览器回归；两端构建通过，OpenNext 产物构建成功。
- 正式服务健康检查和数据库连接正常。
- 在生产服务器执行四个真实 AI 案例，全部通过；Gemini 与 DeepSeek 回退链均有
  成功结果。测试句未进入用户的学习／评分历史。
- 线上桌面及 320 像素手机浏览器检查的截图、报告保存在本地私有工作目录
  `.implementation/production-20260910/`；临时登录仅用于验收，完成后撤销。

## 回退与清理

旧应用文件保留在 `/var/www/jlpt-releases/legacy-before-learning-v2`，仅供恢复文件
和排查；旧版本不支持新的多级别计划约束，不能直接作为数据库升级后的完整回退。
优先按账号关闭 `learningV2Enabled` 并保留新版兼容 API；回退前端时确认其与兼容
后端配套。上一个发布前 Worker 版本为 `da29cb14-42e2-4a85-bebc-c9a6fc1c8ab3`。

备份恢复会覆盖备份之后的新数据，不能当作普通应用回退直接执行。恢复前另做当前
备份并明确需要保留的新记录；不删除新增学习历史或直接反向执行迁移。
演练数据库及临时验收登录在验证结束后清理，生产备份和旧应用目录保留。
