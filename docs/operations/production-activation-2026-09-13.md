# 2026-09-13 生产上线记录

本记录整理根执行代理提供的生产操作与线上验证结果；记录代理没有重新连接生产、执行删除或发布。下列时间均为 UTC。本文记录已完成的 Web/API 上线，不代表 Android、Google Play 或广告已全部可用。

## 已完成：API 与数据安全迁移

- API 版本：`d1d602121253ecd5a2d8e92850acd01c83485617`，于 `2026-09-13T06:04:44Z` 上线。
- 迁移前备份 SHA-256：`384c9985e69ed8cfb7fd9d14944a5d230b61bb594fdce1353dba3a4be2bf6710`。
- 隔离恢复演练与正式迁移验证均通过：数据库累计 19 项迁移；48 张业务表的 20,618 个既有主键保留，包括复合主键和非 `id` 主键。
- `BillingConfig` 与完整首发赠送权益行保持不变，包含归属、状态、日期和使用量字段；验证时 tombstone 数量为 0。
- 数据库原生销售、广告奖励开关及环境中的原生功能开关均保持关闭。
- 未执行真实账号删除，既有用户未被删除。上线的是删除安全保护与可审查的操作能力，不能据此宣称已处理任何删除申请。
- 根执行代理报告主代码相关 PostgreSQL 回归共 166 项通过。

首次恢复演练在 `pg_restore` 输入阶段出现 `EPIPE`。恢复脚本随后改为打开备份文件描述符作为标准输入，并在 `finally` 中关闭文件，不忽略该错误。失败恢复库已确认清理后才再次演练；修复后的恢复演练通过。正式迁移和版本切换发生在成功演练之后，未用失败结果放行。

## 已完成：Web 页面与隐私入口

- Web 版本：`a7b1ce25c80867350ec128b70b590b7350e435c7`。
- 已上线 Worker 版本：`454e1f45-1169-4d6a-9070-85b306ad2ca5`。
- 根执行代理已在线检查中英文隐私页、账号与数据删除申请页的标题和链接。
- 删除页面提供人工申请渠道，不代表已有一键删除、已实际删除或承诺固定处理时限。具体申请仍须按操作流程核验主体、范围和必要保留事项。

## Google 通知与 AdMob 当前状态

永久 `jlpt-play-rtdn-live` 订阅已配置为带 OIDC 身份验证的 Push，使用既有服务账号并接收整个配置主题的通知。`2026-09-13T06:06:04.755Z` 的真实 live TEST 通知已记录为 `PROCESSED`，数量为 1。测试订阅已保存为 Pull，保存成功。该 TEST 证明通知投递与处理链路，不等同于所有购买、退款场景均已完成生产验证。

AdMob 正式 SSV 地址通过平台设置工具测试时，因为没有真实有效 ticket 返回 400。未放宽 ticket 校验，也未伪造生产奖励。自有广告位的临时回调地址已清空，保存后重新打开确认；永久 SSV 设置尚未完成，广告开关继续关闭。

AdMob CMP 草稿已加入正式 `/privacy` 地址，但草稿未发布。`app-ads.txt` 已通过验证，应用审核仍为 pending。上述结果不能表述为广告已上线或隐私同意流程已完成。

## 临时验证环境已收尾

- 根执行代理的临时代理、隧道及 Web 3059 服务已停止。
- A 代理于 `06:11:59.946Z` 归档并停止测试 API 4401 与 relay 4403。根执行代理复核 3059、4401、4402、4403 均无监听。最终隔离数据库、私有状态和停止证明已保存在工作区受保护的 `.local/platform-evidence-20260913`，目录权限 700、文件权限 600。
- E 代理创建的两个 AVD 已关闭，磁盘保留。
- Pad 最初离线；用户重新连接后，于 `06:17:02Z` 保留数据恢复同版本、同上传证书的 `2.0.0-twa-preview`。原网页登录态和正式学习首页正常，中文标题正确，顶部无浏览器关闭、地址、分享及菜单栏；根执行代理独立查看实机截图确认。本次专用 instrumentation 包已核对哈希后卸载，屏幕设置与原值相同。当前预览的原生购买和广告仍关闭，不能等同于已上架版本。

## 后续更新：半年首发与成人受众

- 用户最终选择仅面向 **18 岁以上**，并亲自启用 Google 判定未成年人限制。根代理在 Play Console 摘要页核实“更改已保存”；尚未随新版送审。此前全年龄及 13+ 讨论均被取代。
- 用户将首发期限改为 **六个日历月**，面向所有可销售地区。原 `launchAt=2026-09-12T23:58:15.676Z` 保留；API 截止改为 `2027-03-12T23:58:15.676Z`。
- Google Play 的 `launch-64` 截止已保存为 `2027-03-12T23:58:00Z`。独立只读 API 于 `06:40:07Z` 核实优惠 ACTIVE、173/173 当前地区 AVAILABLE；购买选项的未来新增地区为 AVAILABLE，优惠本身没有该字段，不能推定未来地区自动加入优惠。
- API 版本 `76a8eaf849532a0847de25fca2a5fbc2ec07d203` 于 `06:43:42Z` 上线。此次仅更新代码，无数据库迁移；数据库配置、原启动时间和原生关闭开关均核实未变。公开健康检查正常，日卡 99 美分、首发年卡 6400 美分、新截止时间均已核实。
- 六个月采用 UTC 日历月计算，月底不足取月末；6 个日期单元测试及 38 个 PostgreSQL 验收测试通过。旧结账报价/成交快照与 365 天赠送规则保留。

## 后续更新：正式广告回调与真机年卡查询

- API `86b3ee513c354d079b741577d54994b6134f3dbd` 于 `06:50:11Z` 上线，仍为无迁移的代码发布。Google 签名校验成功且确实没有 `custom_data` 字段的通知，仅确认收到，不访问奖励数据库、不发奖；空值、错误或未知票据继续拒绝。22 项相关测试通过，包含真实 ECDSA、独立 PostgreSQL、篡改拒绝和并发奖励幂等。
- 根代理通过 AdMob 正式网址验证工具得到“成功！该回调网址已通过验证”，随后保存并重新展开核实网址为 `https://api.jlpt.meritledger.org/api/v1/android/commerce/admob/ssv`。仅设置专用广告位 `7191979288`，没有应用到其他广告联盟。
- `JLPT Android — European consent` 已在 AdMob 显示“已发布”，仅关联 Android `com.meritledger.app`。默认英语、另加中文和日语；EEA/英国/瑞士显示；同意、不同意、管理选项均启用。平台提示最多一小时分发。实际 SDK 同意/拒绝/撤回验收继续进行，正式广告开关仍关闭。
- 用户重新连接真机并授权调试后，E 的独立 instrumentation 只读查询返回：日卡 `buy/null` JPY 150；年卡 `buy/null` JPY 15200 与 `buy/launch-64` JPY 9824。SDK 连接、功能和 BillingConfig 响应成功，JP 匹配，无 unfetched 项。这证明当前优惠可见，不证明原 `status 4` 的根因，也不等同于完成年卡结账。
- Web `04aac198bcc5e60bbb547161f705843124fdccc3`（产品源码与构建点 `891f32e` 相同，后两次提交仅测试/报告）已发布为 Worker `676d1d0a-ae5a-46ec-97f5-264873fee4a7`。根代理在线查看 `/login`：18+ 复选框默认未选、继续按钮禁用；点击隐私链接可直接访问公开说明，返回仍未确认；浏览器无相关错误。候选 94 项单测、4 项桌面/320px 浏览器检查及构建通过。该确认仅为当前标签页自我声明，不是年龄证明或后端身份授权。
- Play 数据安全的删除申请网址已改为 `https://jlpt.meritledger.org/delete-account`，界面显示“已保存您所做的更改”；这是草稿，尚未提交新版声明。

## 仍待完成，未放行 Android 发布

- 年卡当前真机已正常返回，实际年卡测试结账、验证与退款仍需完成。
- 目标受众已定为 18+；Web 的成人使用确认与 AI 服务适用条件仍需落实，不能将商店限制等同于 Web 年龄核验。
- 新 JLPT 服务的审核登录方式、最终 AAB、旧服务入口路径与 UMP 尚待完成或核验；旧服务登录说明不能自动视作新服务的有效审核账号方案。
- 永久 SSV 配置已完成；广告平台审核、更新后的 SDK 隐私流程及正式奖励链路仍待完成。
- 本轮没有向 Google Play 上传或发布版本。

后续发布应以这些剩余项目的实际证据为准，继续保留原生开关关闭状态，直至相应功能验收完成。
